package statistics

/*
import (
	"github.com/Northern-Lights/os-rules-engine/network"
	"github.com/Northern-Lights/os-rules-engine/rules"
	"github.com/Northern-Lights/os-rules-engine/ui"
)

// Collector collects statistics on network connections
type Collector interface {
	OnConnectionEvent(conn *network.Connection, match *rules.Rule, missed bool)
	OnDNSResponse()
	OnIgnored()
	Serialize() *ui.Statistics
	Start()
}
*/
